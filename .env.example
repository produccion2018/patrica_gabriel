# Copiar este archivo como ".env" en local si querés probar apuntando a un backend
# que no sea localhost:5000. En Vercel, esta misma variable se carga desde el
# panel de "Environment Variables" del proyecto (no se sube el .env).

VITE_API_URL=https://tu-backend-en-render.onrender.com
